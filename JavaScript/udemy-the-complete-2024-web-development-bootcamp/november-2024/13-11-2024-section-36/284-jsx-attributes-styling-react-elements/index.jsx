import React from "react";
import ReactDOM from "react-dom/client";

ReactDOM.createRoot(document.getElementById("root")).render(
  <div>
    <h1>Hello World!</h1>
    <p>This counts as single HTML element cause its one div.</p>
  </div>
);
